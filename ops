{"author":{"id":"a3a282966ced55effb4560271fcd98e13241af24ea76b00ec76a0f18154bc8ee"},"ops":[{"type":3,"timestamp":1790285326,"nonce":"tlbrcyQbe3MwYPqq+06NMbZbCc8=","message":"Landed 2026-09-24 (list MVP, no nesting): package tui on Bubble Tea v2 draws list and show behind git work view and flow run; package view carries the per-kind argument tables in three tiers (query on every kind but show; list fields/details/group_by/expand/depth/rank; board columns/values/card/group_by/rank; gantt start/stop/label/scale/from/to/progress/group_by/expand/depth/rank; show id/fields); host.View is the one code path for the command and work.view.*; package rank has Between for the drop key. Keys: arrows, vim and emacs at once; Space grabs when rank is bound; e edits by schema kind (picker ends with a clearing (none), title cannot be cleared); c comments; y yanks over OSC 52; / filters locally; t on show toggles the op log. The watcher refreshes the query and keeps the cursor on the id. Verified: a second process created and set issues in under 100ms while a view was open and the view redrew with them, so the renderer holds no lock. board, gantt, expand and depth are parsed and refused naming this renderer. Still open here: board and gantt after the migration (bf6f392), then nesting. Calls made in code and recorded in AGENTS.md: flow run --format text prints a string bare and a list one per line; view KIND - reads kwargs from stdin; the op-log line on show is \u003cop id\u003e \u003ctime\u003e \u003ctype\u003e \u003csummary\u003e.","files":null}]}