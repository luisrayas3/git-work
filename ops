{"author":{"id":"a3a282966ced55effb4560271fcd98e13241af24ea76b00ec76a0f18154bc8ee"},"ops":[{"type":3,"timestamp":1790100753,"nonce":"j7gCm7I43UrQWIGJRWJIGE5TrbE=","message":"Started 2026-09-22 as a PEER of entities/bug (Luis: duplicate bug as issue, implement the new model in it, migrate this project, then delete bug). Landed, uncommitted at time of writing: entities/issue — formatVersion 5, Typename issue, Namespace issues-v5 (provisional; two formats can't share a namespace because the version gate rejects the other's packs); ops Create(1, byte-identical to bug's for the same inputs, plus optional fields), AddComment(3), EditComment(6), NoOp(7), SetMetadata(8), SetField(9), AddRelation(10), RemoveRelation(11); codes 2/4/5 retired and never reused; validation is structural only: keys ^[a-z][a-z0-9_-]*$ (≤64), values valid JSON (≤64 KiB), title a non-empty one-line string that null can't clear; null clears any other field. cache/issue_*.go — IssueCache, IssueExcerpt carrying Fields and Relations whole, RepoCacheIssue with Query (status filters refused until 3c9c24d names categories; --label maps onto the labels field for now), wired as a second subcache so push/pull/merge cover refs/issues-v5. commands/issue — new/show/set/relation/comment/rm, explicit ids, no editor, values JSON-or-string. entities/bug's Typename went back to 'bug' (in-process only), commands/bug lost its 'issue' alias. entities/issue TestIdsMatchBug pins the id-preservation property bf6f392 needs. Still open on this task: schema validation on the cache write path (arrives with bb9e89e), rank helpers (441dcbb), the e8d6426 JSON Patch shape; the delete of bug belongs to bf6f392.","files":null}]}