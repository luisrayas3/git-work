{"author":{"id":"7674679dac9170669f752d932f8b5955b1d55717359e13b1c1e687003c7bb99f"},"ops":[{"type":3,"timestamp":1789868070,"nonce":"x1d/Er+1ckyICpPzPvYpTxm7geI=","message":"Two kinds missing from the list above, found while reviewing the design (6555e36):\n\n- `multi-enum` — the stated kinds (enum-with-category, ordinal-enum) are both single-valued, so Jira's components, fixVersions, affectedVersions and every multi-select custom field have nowhere to go but labels, which flattens away which field a value came from. Worth settling at the same time: is `labels` simply the built-in instance of multi-enum, so there is one multi-select mechanism rather than two?\n- `multi-identity` — same shape over identity values. Assignee is single in both Jira and Linear, but reviewers and watchers are not.\n\nBoth are cheap now and awkward to retrofit once presets and the Jira field mapping (69b7be0) are written against the kind list.\n\nManual rank is a third gap but not just a missing kind — it needs its own merge semantics, so it is split out as 441dcbb.","files":null}]}